function countCharacter(str){
  return(str.split("").length)
  // Ensures denary is an integer, converts to hex, makes upper case
}

console.log(countCharacter(prompt("String to count characters: ")))